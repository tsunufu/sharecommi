<!--<section class="center-vertical">
	<h1>最新の投稿</h1>

	<div class="post center-vertical">
		<% if !@userposts.empty? %>
			<% @userposts.each do |userpost| %>
				<p>お店の名前: <%= userpost["shopname"] %></p>
				<img src="<%= userpost["img"] %>" width="125" height="125">
				<p>ジャンル: <%= userpost["category"] %></p>
				<p>評価: <%= userpost["assessment"] %></p>
				<p><a href="<%= userpost["shopurl"] %>">URL</a></p>
				<p>コメント: <%= userpost["comment"] %></p>
				<p>投稿者: <%= User.find_by(id: userpost.user_id).name %></p>
				<% unless current_user.relationships.find_by(followed_id: userpost.user_id ) %>
				    <a href="/home/<%= userpost.user_id %>/follow">フォローする</a>
				<% else %>
				    <a href="/home/<%= userpost.user_id %>/follow_del">フォロー解除</a>
				<% end %>
				<p>いいねしたユーザー</p>
				<% userpost.favorite_users.each do |favorite_user| %>
					<div class="user-box">
						<img src="<%= favorite_user.icon %>" alt="user_image" width="40", height="40">
					</div>
				<% end %>
					<% if !current_user.nil? %>
						<% if !userpost.favorite_users.find_by(id: session[:user]) %>
							<a href="/post/<%= userpost.id %>/like">いいねする</a>
						<% else %>
							<a href="/post/<%= userpost.id %>/like_del">削除</a>
						<% end %>
					<% end %>
			<% end %>
	</div>
	<% else %>
	<p>投稿がありません</p>
	<% end %>
	
</section>-->
	

	<!--<div class="post center-vertical">-->
		<div class="album py-5 bg-light">
    		<div class="container">
    			<div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
					<% if !@userposts.empty? %>
						<% @userposts.each do |userpost| %>
						        <div class="col">
						        	<div class="card shadow-sm">
						            <img class="bd-placeholder-img card-img-top" width="100%" height="225"  src="<%= userpost["img"] %>"><title>Placeholder</title><rect width="100%" height="100%" fill="#55595c"/>
						
								          <div class="card-body">
								          	  <p class="card-text">投稿者: <%= User.find_by(id: userpost.user_id).name %></p><br>
								              <p class="card-text">お店の名前: <%= userpost["shopname"] %></p><br>
								              <p class="card-text">ジャンル: <%= userpost["category"] %></p><br>
								              <p class="card-text">評価: <%= userpost["assessment"] %></p><br>
								              <p class="card-text"><a href="<%= userpost["shopurl"] %>">URL</a></p><br>
								              <p class="card-text">コメント: <%= userpost["comment"] %></p><br>
											  <% if !current_user.nil? %>
											  	<% if !userpost.favorite_users.find_by(id: session[:user]) %>
									              <div class="d-flex justify-content-between align-items-center">
									                <div class="btn-group">
									                  <button type="button" class="btn btn-sm btn-outline-secondary"><a href="/post/<%= userpost.id %>/like">いいねする</a></button>
									                 </div>
									              </div>
									            <% else %>
									              <div class="d-flex justify-content-between align-items-center">
									                <div class="btn-group">
									                  <button type="button" class="btn btn-sm btn-outline-secondary"><a href="/post/<%= userpost.id %>/like_del">削除</a></button>
									                </div>
									              </div>
									             <% end %>
									           <% end %>
									           <% unless current_user.relationships.find_by(followed_id: userpost.user_id ) %>
												    <a href="/home/<%= userpost.user_id %>/follow">フォローする</a>
											   <% else %>
												    <a href="/home/<%= userpost.user_id %>/follow_del">フォロー解除</a>
											   <% end %>
						            		</div>
						          		</div>
						        	</div>
							<% end %>
				</div>
			</div>
		</div>
	<!--</div>-->
			<% else %>
			<p>投稿がありません</p>
			<% end %>

